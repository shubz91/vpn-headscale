Description:
    This contains config for running headscale server and Caddy to reverse proxy
    
Start the system with:
    docker compose up -d

Common commands
- List all nodes: docker exec -it headscale headscale nodes list